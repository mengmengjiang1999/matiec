## 1. Native method syntax

- [x] 1.1 Add FB-owned method AST nodes and experimental lexer/parser rules
- [x] 1.2 Preserve validated method source while retaining compatibility lowering

## 2. Pipeline consumers

- [x] 2.1 Preserve method ownership through dependency ordering and semantic passes
- [x] 2.2 Emit native methods through IEC generation without duplicate C generation

## 3. Verification and documentation

- [x] 3.1 Add AST, normalized IEC, generated C, `-p`, negative, and legacy regressions
- [x] 3.2 Update feature catalogue, architecture/user, and method-semantics documentation
- [x] 3.3 Run focused and full tests plus sanitizer and strict OpenSpec validation
- [x] 3.4 Archive, create the single commit, and push only to `origin/main`
